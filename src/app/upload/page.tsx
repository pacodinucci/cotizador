import PDFUploader from "@/components/global/PDFUploader";
import React from "react";

type Props = {};

const UploadPage = (props: Props) => {
  return (
    <div>
      <PDFUploader />
    </div>
  );
};

export default UploadPage;
